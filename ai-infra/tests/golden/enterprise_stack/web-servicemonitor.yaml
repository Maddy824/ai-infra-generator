# Prometheus ServiceMonitor for web
# Generated by ai-infra
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: web-monitor
  labels:
    app: web
    project: enterprise-app
spec:
  selector:
    matchLabels:
      app: web
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
